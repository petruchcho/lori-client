<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.egorpetruchcho.loriandroid"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".core.LoriApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.LoginActivity"
            android:label="@string/app_name"/>

        <activity
            android:name=".ui.timesheet.TimesheetActivity"
            android:label="@string/app_name"/>

        <activity
            android:name=".ui.timesheet.DayActivity"
            android:label="@string/app_name"/>

        <activity android:name=".ui.timesheet.AddEntryActivity"
                  android:label="@string/app_name"/>

        <service
            android:name="com.egorpetruchcho.loriandroid.background.BackgroundSpiceService"
            android:exported="false"/>
    </application>

</manifest>